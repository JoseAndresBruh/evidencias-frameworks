INSTRUCCIONES PARA EJECUTAR (REACT):

1. Abrir esta carpeta en la terminal.
2. Instalar dependencias (obligatorio):
   Escribir: npm install

3. Correr el proyecto:
   Escribir: npm start
   (Nota: Si "npm start" no funciona, intente con: npm run dev)

4. Abrir en el navegador la dirección que muestre la terminal (usualmente http://localhost:3000 o http://localhost:5173).